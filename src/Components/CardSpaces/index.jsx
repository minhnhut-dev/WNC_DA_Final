import React from 'react';

function CardSpaces({features}) {
  const {id, description, formAdvertising, locationTypes, zone, full_address} = features.properties;
  
  return (
    <>
    </>
  );
}

export default CardSpaces;